create database mvc_avaliacao;
use mvc_avaliacao;

CREATE TABLE LIVROS (
    ID INTEGER AUTO_INCREMENT PRIMARY KEY,
    TITULO varchar(255),
    AUTOR  varchar(255),
    DATA_PUBLICACAO  varchar(255),
    EDITORA  varchar(255)
);